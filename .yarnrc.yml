nodeLinker: node-modules

enableGlobalCache: true

# Use the traditional node_modules layout instead of Plug'n'Play.
# Reason: Docusaurus 3.10 + Node 26 hits ERR_INTERNAL_ASSERTION inside the
# PnP loader (https://github.com/nodejs/node/issues/...) — switching to
# node-modules linker sidesteps the issue cleanly.
